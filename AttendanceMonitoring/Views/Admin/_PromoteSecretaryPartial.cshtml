@using AttendanceMonitoring.ViewModel;
@model PromoteSecretaryViewModel;
@{
    ViewData["Title"] = "Promote Secretary";
}

<form id="PromoteSecretaryForm" asp-action="PromoteSecretary" class="row g-3" method="post">
    @Html.AntiForgeryToken()
    <div class="col-12">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    </div>
    <div class="col-12">
        <label asp-for="SectionId" class="form-label"><strong>Grade & Section</strong></label>
        <select asp-for="SectionId" asp-items="@Model.AvailableGradeSection" id="sectionSelection" class="form-control gradeSectionSelection" required>
            <option value="">Select Grade and Section...</option>
        </select>
        <span asp-validation-for="AvailableGradeSection" class="text-danger validation-error-message"></span>
    </div>
    <br />
    <div class="col-12">
        <dl>
            <dt style="color: black">School ID</dt>
            <dd>@Model.SchoolId</dd>
            <dt style="color: black">Full Name</dt>
            <dd>@Model.FirstName @Model.MiddleName @Model.LastName</dd>
        </dl>
    </div>
    
    <div class="modal-footer col-12">
        <button type="submit" class="btn btn-primary" id="PromoteSecretary">Save</button>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
    </div>
</form>

@section Script {
    @{

        await Html.RenderPartialAsync("_ValidationScriptPartial.cshtml");
    }
}
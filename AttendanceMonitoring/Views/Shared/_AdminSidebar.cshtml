@{
    var isUserSectionActive = Html.IsActive("Admin", "TeacherList", "StudentList", "SecretaryList") == "active";
    var isGradeAndSectionActive = Html.IsActive("Admin", "GradeList", "SectionList") == "active";
}
<!-- Sidebar -->
<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

    <!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center " href="@Url.Action("AdminHome", "Admin")">
        <div class="sidebar-brand-icon">
            <!-- <i class="fas fa-laugh-wink"></i> -->
            <i class="fas fa-home"></i>
        </div>
        <div class="sidebar-brand-text mx-3">Admin Dashboard</div>
    </a>

    <!-- Divider -->
    <hr class="sidebar-divider my-0">
    <!-- Nav Item - Dashboard --> 
                                      @*Url.Action and Html.ActionLink(IsACtive) are helper method*@
    <li class="nav-item  @Html.IsActive("Admin", "AdminHome")">@*Dito nauna ang Controller name bago action name depende sa pag set mo sa custom method mo na IsActive*@
        <a class="nav-link" href="@Url.Action("AdminHome", "Admin")">@*Action name muna bago controllername kapag Url.Action ang gamit, default to*@
            <i class="fas fa-fw fa-tachometer-alt"></i>
            <span>Dashboard</span>
        </a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider">

    <!-- Heading -->
    <div class="sidebar-heading">
        Interface
    </div>

    <!-- Nav Item - Academic Period List -->
    <li class="nav-item @Html.IsActive("Admin", "AcademicPeriodList")">
        <a class="nav-link" href="@Url.Action("AcademicPeriodList", "Admin")">
            <i class="fas fa-calendar"></i>
            <span>Academic Period</span>
        </a>
    </li>

    @* <!-- Nav Item - Grade & Section -->
    <li class="nav-item @Html.IsActive("Admin", "GradeAndSectionList")">
        <a class="nav-link" href="@Url.Action("GradeAndSectionList", "Admin")">
            <i class="fas fa-chalkboard-teacher"></i>
            <span>Grade & Section</span>
        </a>
    </li> *@

    <!-- Nav Item - Grade And Section -->
    <li class="nav-item @(isGradeAndSectionActive ? "active" : "")">
        <a class="nav-link collapsed @(isGradeAndSectionActive ? "active" : "")" href="#" data-toggle="collapse" data-target="#collapseTwo01"
           aria-expanded="true" aria-controls="collapseTwo">
            <i class="fas fa-chalkboard-teacher"></i>
            <span>Academic Classes</span>
        </a>
        <div id="collapseTwo01" class="collapse @(isGradeAndSectionActive ? "show" : "")" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Class Components:</h6>
                <a class="collapse-item @Html.IsActive("Admin", "GradeList")" href="@Url.Action("GradeList", "Admin")">Grade List</a>
                <a class="collapse-item @Html.IsActive("Admin", "SectionList")" href="@Url.Action("SectionList", "Admin")">Section List</a>
            </div>
        </div>
    </li>

    <!-- Nav Item - Subjects -->
    <li class="nav-item @Html.IsActive("Admin", "SubjectList")">
        <a class="nav-link" href="@Url.Action("SubjectList", "Admin")">
            <i class="fa fa-book">‌</i>
            <span>Subjects</span>
        </a>
    </li>

    

    <!-- Nav Item - USERS -->
    <li class="nav-item @(isUserSectionActive ? "active" : "")">
        <a class="nav-link collapsed @(isUserSectionActive ? "active" : "")" href="#" data-toggle="collapse" data-target="#collapseTwo"
           aria-expanded="true" aria-controls="collapseTwo">
            <i class="fas fa-users"></i>
            <span>User List</span> 
        </a>
        <div id="collapseTwo" class="collapse @(isUserSectionActive ? "show" : "")" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">User Components:</h6>
                <a class="collapse-item @Html.IsActive("Admin", "TeacherList")" href="@Url.Action("TeacherList", "Admin")">Teacher List</a>
                <a class="collapse-item @Html.IsActive("Admin", "StudentList")" href="@Url.Action("StudentList", "Admin")">Student List</a>
                <a class="collapse-item @Html.IsActive("Admin", "SecretaryList")" href="@Url.Action("SecretaryList", "Admin")">Secretary List</a>
            </div>
        </div>
    </li> 

    <!-- Nav Item - REPORTS -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseUtilities"
           aria-expanded="true" aria-controls="collapseUtilities">
            <i class="fas fa-fw fa-chart-area"></i>
            <span>Reports</span>
        </a>
        <div id="collapseUtilities" class="collapse" aria-labelledby="headingUtilities"
             data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Report Components:</h6>
                <a class="collapse-item" href="#">Record Lists</a>
                <a class="collapse-item" href="#">Attendace Records</a>
            </div>
        </div>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider">

    <!-- Heading -->
    <div class="sidebar-heading">
        System Settings
    </div>

    <!-- Nav Item - Pages Collapse Menu -->
    <!-- <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsePages"
            aria-expanded="true" aria-controls="collapsePages">
            <i class="fas fa-fw fa-folder"></i>
            <span>Pages</span>
        </a>
        <div id="collapsePages" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Login Screens:</h6>
                <a class="collapse-item" href="login.html">Login</a>
                <a class="collapse-item" href="register.html">Register</a>
                <a class="collapse-item" href="forgot-password.html">Forgot Password</a>
                <div class="collapse-divider"></div>
                <h6 class="collapse-header">Other Pages:</h6>
                <a class="collapse-item" href="404.html">404 Page</a>
                <a class="collapse-item" href="blank.html">Blank Page</a>
            </div>
        </div>
    </li> -->
    <!-- Nav Item - Backup and Restore -->
    <li class="nav-item @Html.IsActive("Admin", "BackupAndRestore")">
        <a class="nav-link" href="@Url.Action("BackupAndRestore", "Admin")">
            <i class="fas fa-fw fa-cog"></i>
            <span>Backup & Restore</span>
        </a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider d-none d-md-block">

    <!-- Sidebar Toggler (Sidebar) -->
    <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
    </div>
</ul> 

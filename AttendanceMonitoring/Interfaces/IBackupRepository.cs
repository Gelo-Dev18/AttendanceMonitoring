using AttendanceMonitoring.Helper;

namespace AttendanceMonitoring.Interfaces
{
    public interface IBackupRepository<T>
    {
        //Task<PaginatedResult<T>> GetPaginated(int page, int pageSize);
    }
}
